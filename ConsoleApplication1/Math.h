#include "tools\MatrixDefs.h"
using namespace matrices;

namespace localmath{
	const Vector3 unitx(1, 0, 0);
	const Vector3 unity(0, 1, 0);
	const Vector3 unitz(0, 0, 1);

	const Vector3 zero(0, 0, 0);

	void vect3ToArray(double[3], const Vector3);
}