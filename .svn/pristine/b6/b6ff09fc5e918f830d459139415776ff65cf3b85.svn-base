
#ifndef _CLASS_IKSOLVERI
#define _CLASS_IKSOLVERI

#include "Exports.h"

namespace manip_core
{
struct TreeI;

struct MANIPCORE_API IKSolverI {

public:
	virtual bool QuickStepClamping(TreeI* /*pTree*/, const double* /*target*/) const = 0; //true if target reached // target in robot coordinates
	virtual bool StepClamping(TreeI* /*pTree*/, const double* /*target*/, const double* /*direction*/) const = 0; //true if target reached // target in robot coordinates
	virtual void Release() = 0;
};

extern "C" MANIPCORE_API IKSolverI* GetIKSolver();
} // namespace manip_core
#endif //_CLASS_IKSOLVERI