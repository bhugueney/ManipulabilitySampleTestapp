
#ifndef _CLASS_INTERSECTION
#define _CLASS_INTERSECTION

#include "MatrixDefs.h"

class Tree;
class Obstacle;

class Intersection {

public:

public:
	 Intersection();
	~Intersection();

public:
	bool Intersect(const matrices::Matrix4& /*treeTransform*/, const Tree& /*tree*/, const Obstacle& /*obstacle*/, matrices::Vector3& /*anInterPoint*/ ) const;

private:
	// do segments AB et CD intersect ?
	bool IntersectSegments( const matrices::Vector2& /*A*/, const matrices::Vector2& /*B*/, const matrices::Vector2& /*C*/, const matrices::Vector2& /*D*/, matrices::Vector3& /*intersectionPoint*/) const;
	float determinant( const matrices::Vector2& /*A*/, const matrices::Vector2& /*B*/, const matrices::Vector2& /*C*/) const;
};

#endif //_CLASS_INTERSECTION