
#ifndef _CLASS_IKSOLVERIMP
#define _CLASS_IKSOLVERIMP

#include "Exports.h"
#include "MatrixDefs.h"

namespace manip_core
{
	struct TreeI;
	struct RobotI;
	struct IKSolverI;
} // namespace manip_core

class IKSolver {

public:
	 IKSolver();
	~IKSolver();

public:
	bool QuickStepClamping(manip_core::TreeI* /*pTree*/, const matrices::Vector3& target) const; //true if target reached // target in robot coordinates
	bool StepClamping(manip_core::TreeI* /*pTree*/, const matrices::Vector3& target, const matrices::Vector3& /*dir*/) const; //true if target reached // target in robot coordinates

	bool QuickStepClampingToTargets(manip_core::RobotI* /*robot*/) const; //true if target reached // target in robot coordinates
	bool StepClampingToTargets(manip_core::RobotI* /*robot*/, const matrices::Vector3& /*dir*/) const; //true if target reached // target in robot coordinates

private:
	manip_core::IKSolverI* solverI_;
};

#endif //_CLASS_IKSOLVERI