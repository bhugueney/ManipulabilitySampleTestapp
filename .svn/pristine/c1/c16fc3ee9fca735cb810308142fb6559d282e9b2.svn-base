
#ifndef _CLASS_INTERSECTION
#define _CLASS_INTERSECTION

#include "kinematic/MatrixDefs.h"

class Tree;
class Obstacle;

class Intersection {

public:

public:
	 Intersection();
	~Intersection();

public:
	bool Intersect(const Tree* /*pTree*/, const Obstacle& /*obstacle*/, matrices::Vector3& anInterPoint ) const;

private:
	// do segments AB et CD intersect ?
	bool IntersectSegments( const matrices::Vector2& /*A*/, const matrices::Vector2& /*B*/, const matrices::Vector2& /*C*/, const matrices::Vector2& /*D*/, matrices::VectorX& /*intersectionPoint*/) const;
	float determinant( const matrices::Vector2& /*A*/, const matrices::Vector2& /*B*/, const matrices::Vector2& /*C*/) const;
};

#endif //_CLASS_INTERSECTION