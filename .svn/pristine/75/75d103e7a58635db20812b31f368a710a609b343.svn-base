
#ifndef _CLASS_IKSOLVER
#define _CLASS_IKSOLVER

#include "MatrixDefs.h"

class Tree;

class IKSolver {

public:
	 IKSolver( const float stepSize = 0.01f, const float treshold = 0.01f );
	~IKSolver();

public:
	bool Step( Tree& tree, const matrices::Vector3& target ); //true if target reached

private:
	const float stepSize_;
	const float treshold_;
};

#endif //_CLASS_IKSOLVER