
#ifndef _CLASS_SCORER
#define _CLASS_SCORER

#include "API/PostureScorerI.h"


#include <vector>

namespace statepostures
{
	struct ScorerHuman : public manip_core::PostureScorerI
	{
		ScorerHuman();
		~ScorerHuman();

		virtual float operator()(const std::vector<manip_core::RobotI*>& postures) const;
		virtual float operator()(const std::vector<bool>& state) const;
	};
}
#endif //_CLASS_SCORER